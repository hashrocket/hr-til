- content_for :page_title, "Posts by #{@developer.username}"
%section#author
  %header.page_head
    %h1
      #{pluralize @developer.post_count, "post"}
      by #{@developer.username}
= render @posts
